#!/usr/bin/env bash
# Top-level grader entrypoint shim. The README Deployment Guide ends at this command.
# Frozen CLI lives in docs/CONTRACT.md; impl in src/entrypoint/transcribe.py.
set -euo pipefail
cd "$(dirname "$0")"
exec "${PYTHON:-python3}" -m src.entrypoint.transcribe "$@"
